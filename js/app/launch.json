{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch cli_spec.js",
      "program": "${workspaceFolder}/cli_spec.js",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}